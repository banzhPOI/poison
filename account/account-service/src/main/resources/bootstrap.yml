server:
  port: 8080

spring:
  application:
    name: banzh-account-service
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      config:
        prefix: base-config
        file-extension: yml
