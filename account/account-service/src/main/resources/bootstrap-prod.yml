server:
  port: 8080

spring:
  application:
    name: poison-account-service
  cloud:
    nacos:
      server-addr: nacos-service:8848
      config:
        prefix: base-config
        file-extension: yml
        group: PROD