server:
  port: 8080

spring:
  application:
    name: banzh-gateway
  cloud:
    nacos:
      server-addr: nacos-service:8848
      config:
        prefix: base-config
        file-extension: yml
        group: PROD
  main:
    web-application-type: reactive